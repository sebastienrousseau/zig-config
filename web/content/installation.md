---
title: "Installation — @sebastienrousseau/zig-config"
description: "How to install and set up @sebastienrousseau/zig-config."
layout: "doc"
---

# Installation

Install `@sebastienrousseau/zig-config` as a development dependency using your preferred package manager.

## npm

```bash
npm install --save-dev @sebastienrousseau/zig-config
```

## pnpm

```bash
pnpm add -D @sebastienrousseau/zig-config
```

## yarn

```bash
yarn add -D @sebastienrousseau/zig-config
```

## bun

```bash
bun add -d @sebastienrousseau/zig-config
```
