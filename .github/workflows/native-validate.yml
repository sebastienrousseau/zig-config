# SPDX-FileCopyrightText: 2026 Sebastien Rousseau <sebastian.rousseau@gmail.com>
# SPDX-License-Identifier: Apache-2.0 OR MIT

# The presets in this package configure a toolchain that does not read npm,
# so the only real validation is the toolchain itself.
name: Native Validate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  native:
    name: Toolchain accepts the presets
    uses: sebastienrousseau/config/.github/workflows/reusable-native-validate.yml@033324fc70d73f811f89bce66656a70eab1630f7 # main
    with:
      language: zig
