# `@sebastienrousseau/zig-config` Architecture

Shareable Zig configuration providing standardized Zig Language Server (ZLS) and build formatting rules adhering to modern 2026 standards.

## Design Principles
- Single source of truth for Zig fleet configurations.
- Multi-format exports (CommonJS, ESM, TypeScript declarations).
- 100% test coverage with zero runtime production dependencies.
